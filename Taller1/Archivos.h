#ifndef ARCHIVOS_H_INCLUDED
#define ARCHIVOS_H_INCLUDED
#include "stringdinamico.h"
boolean existe (strings nomArch);

boolean vacio (strings nomArch);

boolean pertenece(strings nomArch);

#endif // ARCHIVOS_H_INCLUDED
